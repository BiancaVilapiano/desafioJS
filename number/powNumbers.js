//Faça uma função chamada powNumbers que recebe dois parâmetros chamados base e exp do tipo number e retorna um number com a exponenciação dos parâmetros;

export function powNumbers(base, exp){
    return base ** exp;
}
