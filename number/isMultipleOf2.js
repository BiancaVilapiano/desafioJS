//Faça uma função chamada isMultipleOf2 que recebe um parâmetro chamado val do tipo number e retorna um boolean indicando se o número é um multiplo de 2;
export function isMultipleOf2(val){
    return (val % 2 == 0);
}
